"use client"

import { useState } from "react"
import { Bot, Building2, Save, UserRound } from "lucide-react"

export default function SettingsClient({ initialSettings }: { initialSettings: any }) {
  const config = initialSettings?.aiConfig
  const [form, setForm] = useState({ agentName: config?.agentName || "AI Sales Agent", businessName: config?.businessName || "", businessDescription: config?.businessDescription || "", tone: config?.tone || "professional", languages: config?.languages || "en", aiInstructions: config?.aiInstructions || "" })
  const [saving, setSaving] = useState(false)
  const [message, setMessage] = useState("")
  async function save() {
    setSaving(true); setMessage("")
    try { const { updateAIConfig } = await import("@/lib/actions/settings"); const result = await updateAIConfig(form); setMessage(result.error || "AI settings saved.") } catch { setMessage("Unable to save AI settings right now.") } finally { setSaving(false) }
  }
  return <div className="mx-auto max-w-5xl space-y-8"><div><p className="text-sm font-semibold uppercase tracking-[0.16em] text-emerald-700">Workspace preferences</p><h1 className="mt-3 text-3xl font-semibold tracking-[-0.03em] text-slate-950">Settings</h1><p className="mt-2 text-sm text-slate-500">Manage your AI Lead Machine workspace and preferences.</p></div><div className="grid gap-4 md:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-emerald-50 text-emerald-700"><Building2 size={19} /></div><h2 className="mt-5 text-base font-semibold text-slate-950">Workspace</h2><p className="mt-2 text-sm leading-6 text-slate-500">Your organization workspace is the security boundary for every lead, property, conversation, and appointment.</p><div className="mt-5 border-t border-slate-100 pt-4 text-xs font-semibold text-slate-700">{initialSettings?.organization?.name || "Workspace"}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-sky-50 text-sky-700"><UserRound size={19} /></div><h2 className="mt-5 text-base font-semibold text-slate-950">Profile</h2><p className="mt-2 text-sm leading-6 text-slate-500">Signed-in workspace identity used for assignments and audit history.</p><div className="mt-5 border-t border-slate-100 pt-4 text-xs font-semibold text-slate-700">{initialSettings?.user?.name || initialSettings?.user?.email}</div></section></div><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2 text-emerald-700"><Bot size={19} /><h2 className="text-base font-semibold text-slate-950">AI Settings</h2></div><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">These server-side settings guide qualification conversations. Property facts always come from your actual inventory.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Agent name<input value={form.agentName} onChange={(event) => setForm({ ...form, agentName: event.target.value })} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-emerald-500" /></label><label className="text-sm font-medium text-slate-700">Business name<input value={form.businessName} onChange={(event) => setForm({ ...form, businessName: event.target.value })} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-emerald-500" /></label><label className="text-sm font-medium text-slate-700">Tone<select value={form.tone} onChange={(event) => setForm({ ...form, tone: event.target.value })} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-emerald-500"><option value="professional">Professional</option><option value="friendly">Friendly</option><option value="casual">Casual</option></select></label><label className="text-sm font-medium text-slate-700">Languages<input value={form.languages} onChange={(event) => setForm({ ...form, languages: event.target.value })} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-emerald-500" /></label></div><label className="mt-4 block text-sm font-medium text-slate-700">Business description<textarea value={form.businessDescription} onChange={(event) => setForm({ ...form, businessDescription: event.target.value })} rows={3} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 text-sm outline-none focus:border-emerald-500" /></label><label className="mt-4 block text-sm font-medium text-slate-700">Additional AI instructions<textarea value={form.aiInstructions} onChange={(event) => setForm({ ...form, aiInstructions: event.target.value })} rows={3} placeholder="Example: always ask for a preferred visit time before scheduling." className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 text-sm outline-none focus:border-emerald-500" /></label><div className="mt-5 flex items-center justify-between gap-4 border-t border-slate-100 pt-5"><p className="text-sm text-slate-500">{message}</p><button onClick={save} disabled={saving} className="inline-flex items-center gap-2 rounded-xl bg-[#07111f] px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50"><Save size={16} />{saving ? "Saving..." : "Save AI settings"}</button></div></section></div>
}
